<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/pull_to_refresh_header"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:background="#ffe7e7e7"
    android:paddingBottom="10.0dip"
    android:paddingTop="10.0dip" >

    <ProgressBar
        android:id="@+id/pull_to_load_progress"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginRight="20.0dip"
        android:layout_marginTop="10.0dip"
        android:layout_toLeftOf="@+id/pull_to_load_image"
        android:indeterminate="true"
        android:progressDrawable="@drawable/refresh_footer_progressbar"
        android:visibility="gone" />

    <ImageView
        android:id="@+id/pull_to_load_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginRight="12.0dip"
        android:layout_toLeftOf="@+id/pull_to_load_text"
        android:alpha="0.3"
        android:gravity="center"
        android:src="@drawable/ic_pulltorefresh_arrow_up"
        android:visibility="visible" />

    <TextView
        android:id="@+id/pull_to_load_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:text="上拉加载更多"
        android:textAppearance="?android:textAppearanceMedium"
        android:textColor="#ff909496"
        android:textSize="14.0sp" />

</RelativeLayout>